// Every piece of branding lives here so it can be changed in one place.
//
// The logo is a pure wordmark, which means the app name IS the logo — changing
// `name` below re-renders the mark everywhere it appears, with no asset files
// to redraw.

export const BRAND = {
  // Changing this one string updates the logo, the browser tab and every
  // header together — there are no image assets to redraw.
  name: 'PLAYBACK',
  tagline: 'LISTENING RECORD',
}
